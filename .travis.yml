language: python
sudo: true

env:
    - ARCH=x64

script:
    - "sudo bash -ex travis-ci.sh"

deploy:
  provider: releases
  api_key: "GITHUB OAUTH TOKEN"
  file: "FILE TO UPLOAD"
  skip_cleanup: true
  on:
    tags: true
